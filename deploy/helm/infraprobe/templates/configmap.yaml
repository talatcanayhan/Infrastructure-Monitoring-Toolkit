apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "infraprobe.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "infraprobe.labels" . | nindent 4 }}
    app.kubernetes.io/component: config
data:
  config.yml: |
    {{- .Values.config | nindent 4 }}
